console.log("Hello from src/index.js!");

// Clearbit API
// Let’s play with the Enrichment feature of Clearbit and its API:

// 👉 clearbit.com/docs?shell#enrichment-api

// Hints
// By default, when you click on a form’s submit button, the page will be reloaded.
// With javascript you can prevent this default behavior by using the .preventDefault() function.
// You can pass HTTP Headers to fetch. In clearBit:
// fetch(url, { headers: { Authorization: Bearer sk_YOUR_API_KEY } }
